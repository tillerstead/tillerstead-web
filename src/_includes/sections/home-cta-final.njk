{# Homepage Final CTA Include Displays the final call-to-action section Usage: {% include "sections/home-cta-final.njk" %} #} {%- set title = title | default("Ready for a Bathroom
Built the Right Way?") -%} {%- set summary = summary | default("Request a free, no-pressure estimate. We")ll
walk the space, discuss options, and explain the assembly—prep, waterproofing, and finish—so you know exactly what
you're getting." -%} {%- set phone = site.company.phone_link | default(site.phone) -%} {%- set phone_display = site.company.phone_display | default(site.phone_display) | default("(609)&nbsp;862&#8209;8808") -%}

<section class="ts-section ts-cta-final bg-primary text-white" aria-labelledby="final-cta-title">
  <div class="container text-center">
    <h2 id="final-cta-title" class="heading-2">{{ title }}</h2>
    <p class="lead">{{ summary }}</p>

    <div class="cta-actions">
      <a href="{{ '/contact/' | relative_url }}" class="btn btn--secondary">Request Free Estimate</a>
      {% if phone %}
      <a href="tel:{{ phone | e }}" class="btn btn--ghost-white"> Call {{ phone_display }} </a>
      {% endif %}
    </div>
  </div>
</section>

