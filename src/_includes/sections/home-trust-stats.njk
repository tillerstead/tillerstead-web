{# Homepage Trust Stats Include Displays credentials and trust indicators Usage: {% include "sections/home-trust-stats.njk" %} #} {%- set eyebrow = eyebrow | default("Southern New
Jersey") -%} {%- set title = title | default("Licensed, Insured & Local") -%} {%- set summary =
summary | default("Clear scope, clean execution, and standards-referenced installation—explained in plain
English.") -%}

<section class="ts-section ts-local-trust" aria-labelledby="local-trust-title">
  <div class="container">
    <!-- Section Header -->
    <header class="section-header text-center">
      <span class="eyebrow">{{ eyebrow }}</span>
      <h2 id="local-trust-title" class="heading-2">{{ title }}</h2>
      <p class="lead">{{ summary }}</p>
    </header>

    <!-- Trust Stats Cards -->
    <div class="cards cards--4col">
      <div class="card card--stat text-center">
        <p class="stat-number text-primary">TCNA</p>
        <p class="stat-label">Referenced Methods</p>
      </div>

      <div class="card card--stat text-center">
        <p class="stat-number text-primary">2-Year</p>
        <p class="stat-label">Workmanship Warranty</p>
      </div>

      <div class="card card--stat text-center">
        <p class="stat-number text-primary">Insured</p>
        <p class="stat-label">Full Liability Coverage</p>
      </div>

      <div class="card card--stat text-center">
        <p class="stat-number text-primary">NJ HIC</p>
        <p class="stat-label">#13VH10808800</p>
      </div>
    </div>
  </div>
</section>
