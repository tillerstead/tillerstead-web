# Legacy Tillerstead Platform Repo

Existing legacy repo:

`tillerstead/Platform`

Current rule:

* Preserve it until `tillerstead/tillerstead-web` is live and verified.
* Do not archive, rename, delete, or modify it until explicit approval.
* After cutover and rollback window, archive or rename it with a clear legacy notice.
