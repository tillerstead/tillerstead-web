{%- set home = home -%}
<section class="home-simple__hero">
  <div class="home-simple__container">
    <div class="home-simple__hero-content">
      <p class="home-simple__eyebrow">{{ home.hero.eyebrow }}</p>
      <h1 class="home-simple__title">{{ home.hero.title }}</h1>
      <p class="home-simple__subtitle">{{ home.hero.subtitle }}</p>
      <p class="home-simple__summary">{{ home.hero.summary }}</p>
      <div class="home-simple__actions">
        <a class="btn btn-primary" href="{{ home.hero.primary.url | relative_url }}">{{ home.hero.primary.label }}</a>
        <a class="btn btn-secondary" href="{{ home.hero.secondary.url | relative_url }}"
          >{{ home.hero.secondary.label }}</a
        >
      </div>
      {% if home.hero.facts %}
      <ul class="home-simple__facts">
        {% for fact in home.hero.facts %}
        <li class="home-simple__fact">
          {%- set fact_icon = "icon-" ~ fact.icon -%} {% include "components/ts-icon.njk" %}
          <div>
            <span class="home-simple__fact-label">{{ fact.label }}</span>
            <span class="home-simple__fact-text">{{ fact.text }}</span>
          </div>
        </li>
        {% endfor %}
      </ul>
      {% endif %}
    </div>
    {% if home.hero.highlights %}
    <div class="home-simple__hero-highlights">
      <h2 class="home-simple__highlights-title">What we verify</h2>
      <ul class="home-simple__highlights">
        {% for highlight in home.hero.highlights %}
        <li>{{ highlight }}</li>
        {% endfor %}
      </ul>
    </div>
    {% endif %}
  </div>
</section>

<section class="home-simple__services">
  <div class="home-simple__container">
    <div class="section-header">
      <p class="section-eyebrow">{{ home.services.eyebrow }}</p>
      <h2>{{ home.services.title }}</h2>
      <p>{{ home.services.summary }}</p>
    </div>
    <div class="home-simple__cards">
      {% for service in home.services.cards %}
      <article class="home-simple__card">
        {%- set service_icon = "icon-" ~ service.icon -%} {% include "components/ts-icon.njk" %}
        <h3>{{ service.title }}</h3>
        <p>{{ service.description }}</p>
        <ul>
          {% for detail in service.details %}
          <li>{{ detail }}</li>
          {% endfor %}
        </ul>
        <a class="home-simple__card-link" href="{{ service.url | relative_url }}">Learn more</a>
      </article>
      {% endfor %}
    </div>
    <p class="home-simple__note">{{ home.services.homeowner_note }}</p>
    <div class="section-action">
      <a class="btn btn-primary" href="{{ home.services.actions.primary.url | relative_url }}"
        >{{ home.services.actions.primary.label }}</a
      >
      <a class="btn btn-secondary" href="{{ home.services.actions.secondary.url | relative_url }}"
        >{{ home.services.actions.secondary.label }}</a
      >
    </div>
  </div>
</section>

<section class="home-simple__process">
  <div class="home-simple__container">
    <div class="section-header">
      <p class="section-eyebrow">{{ home.process.eyebrow }}</p>
      <h2>{{ home.process.title }}</h2>
      <p>{{ home.process.summary }}</p>
    </div>
    <div class="home-simple__steps">
      {% for step in home.process.steps %}
      <article class="home-simple__step">
        {%- set step_icon = "icon-" ~ step.icon -%} {% include "components/ts-icon.njk" %}
        <h3>{{ step.title }}</h3>
        <p>{{ step.description }}</p>
      </article>
      {% endfor %}
    </div>
    <p class="home-simple__fact">{{ home.process.fun_fact }}</p>
  </div>
</section>

<section class="home-simple__materials">
  <div class="home-simple__container">
    <div class="section-header">
      <p class="section-eyebrow">{{ home.materials.eyebrow }}</p>
      <h2>{{ home.materials.title }}</h2>
      <p>{{ home.materials.summary }}</p>
    </div>
    <div class="home-simple__cards">
      {% for item in home.materials.items %}
      <article class="home-simple__card">
        {%- set item_icon = "icon-" ~ item.icon -%} {% include "components/ts-icon.njk" %}
        <h3>{{ item.title }}</h3>
        <p>{{ item.description }}</p>
        <ul>
          {% for point in item.points %}
          <li>{{ point }}</li>
          {% endfor %}
        </ul>
      </article>
      {% endfor %}
    </div>
    <p class="home-simple__note">{{ home.materials.callout }}</p>
  </div>
</section>

<section class="home-simple__testimonials">
  <div class="home-simple__container">
    <div class="section-header">
      <p class="section-eyebrow">{{ home.testimonials.eyebrow }}</p>
      <h2>{{ home.testimonials.title }}</h2>
    </div>
    {%- set reviews = home.testimonials.reviews | default(reviews.reviews) -%} {%- set review_limit = home.testimonials.limit | default(3) -%}
    <div class="home-simple__cards">
      {% for review in reviews | slice(0, review_limit) %}
      <article class="home-simple__card home-simple__testimonial">
        <div class="home-simple__testimonial-quote">{{ review.quote_html }}</div>
        <p class="home-simple__testimonial-author">
          <strong>{{ review.author }}</strong>
          {% if review.job_type %}
          <span>· {{ review.job_type }}</span>
          {% endif %}
        </p>
        {% if review.source %}
        <a class="home-simple__card-link" href="{{ review.url }}" rel="noopener" target="_blank"
          >{{ review.source }} review</a
        >
        {% endif %}
      </article>
      {% endfor %}
    </div>
  </div>
</section>

<section class="home-simple__cta">
  <div class="home-simple__container">
    <div class="home-simple__cta-card">
      <p class="home-simple__eyebrow">{{ home.cta.eyebrow }}</p>
      <h2>{{ home.cta.title }}</h2>
      <p>{{ home.cta.summary }}</p>
      <div class="home-simple__actions">
        <a class="btn btn-primary" href="{{ home.cta.primary.url | relative_url }}">{{ home.cta.primary.label }}</a>
        <a class="btn btn-secondary" href="{{ home.cta.secondary.url }}">{{ home.cta.secondary.label }}</a>
      </div>
      <p class="home-simple__cta-note">{{ home.cta.note }}</p>
    </div>
  </div>
</section>
